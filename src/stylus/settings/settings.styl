//color variables
$accentColor = #435993
$blackSemi = rgba(0, 0, 0, .5)
$blackSignal = #2b2b2b
$blackGraphite = #1c1c1c
$white = #fff
$semiWhite =  rgba(255, 255, 255, .5)
$grayLight = #d9d9d9
$brightTelegray = #c8c8c8
$gainsborough = #E5E5E5
$blue = #50649a
$wetAsphalt = #494949
$transparent = transparent


//numeric variables
$headerHeight = 80px
$headerMobileHeight = 60px
$inputHeight = 60px
$inputHeightMobile = 45px
$topBlockHeight = 180px
$btnHeight = 50px

//mixins
elementPosition(position, top, left)
	position position
	top top
	left left

sizeElement(width, height)
	width width
	height height

linear-interpolation($map)
	if (length(keys($map)) != 2)
		error('Map must have exactly 2 values.')
	$keys   = keys($map)
	$values = values($map)
	$max-width = unit(pop($keys), '')
	$max-size  = unit(pop($values), '')
	$min-width = unit(pop($keys), '')
	$min-size  = unit(pop($values), '')
	$slope       = unit(($max-size - $min-size) / ($max-width - $min-width), '')
	$y-intercept = $min-size - $slope * $min-width
	$sign = \+
	if $y-intercept < 0
		$sign = \-
		$y-intercept = abs($y-intercept)
	unquote('calc(' + unit($slope * 100, vw) + ' ' + $sign + ' ' + unit($y-intercept, px) + ')')

//extends
$clearBoth
	content ''
	display block
	clear both

$pseudoElement
	content ''
	display block
	position absolute

$backgroundPosition
	background-size cover
	background-position center

$absoluteElementPosition
	position absolute
	top 50%
	left 50%

$centerElement
	transform translate(-50%, -50%)

$centerElementY
	transform translateY(-50%)

$centerElementX
	transform translateX(-50%)

$centerElementMargin
	margin 0 auto

$centerText
	text-align center

$robotoRegular
	font-family "Roboto-Regular"

$robotoThin
	font-family "Roboto-Thin"

$smallerFontSize
	@media screen and (min-width: 1170px)
		font-size 13px
	font-size linear-interpolation((320px 10px) (991px 11px))

$smallFontSize
	@media screen and (min-width: 1170px)
		font-size 14px
	font-size linear-interpolation((320px 11px) (991px 12px))

$defaultFontSize
	@media screen and (min-width: 1170px)
		font-size 16px
	font-size linear-interpolation((320px 13px) (991px 14px))

$averageFontSize
	@media screen and (min-width: 1170px)
		font-size 20px
	font-size linear-interpolation((320px 14px) (991px 16px))

$mediumFontSize
	@media screen and (min-width: 1170px)
		font-size 24px
	font-size linear-interpolation((320px 15px) (991px 20px))

$middleFontSize
	@media screen and (min-width: 1170px)
		font-size 30px
	font-size linear-interpolation((320px 22px) (991px 26px))

$bigFontSize
	@media screen and (min-width: 1170px)
		font-size 70px
	font-size linear-interpolation((320px 52px) (991px 58px))

$transitionDefault
	transition all .3s

$transitionSlow
	transition all .5s

$relativePosition
	position relative

$cursorPointer
	cursor pointer
